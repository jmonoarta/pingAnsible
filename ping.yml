- name: Check compability with shell script
  hosts: all
  gather_facts: yes
  
  tasks:
  - name: Check hostname
    shell:  echo "Hostname: " | tr -d "\n" && uname -a | cut -d ' ' -f 2 | tr -d "\n" && echo " OS: " | tr -d "\n" && uname -a  | cut -d ' ' -f 1  > test.txt
    
 
